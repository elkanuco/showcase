<svg width="985px" height="530px" style="background-color: #161b22;" xmlns="http://www.w3.org/2000/svg">
  <style>
    .quote-text {
      font-family: monospace;
      font-size: 24px;
      fill: #0f0;
      text-anchor: middle;
      dominant-baseline: central;
    }
    .author-text {
      font-family: monospace;
      font-size: 18px;
      fill: #0f0;
      text-anchor: middle;
      dominant-baseline: central;
    }
    .attribution-text {
      font-family: monospace;
      font-size: 14px;
      fill: #0f0;
      text-anchor: middle;
    }
  </style>
  <text id="quote" class="quote-text" x="492.5" y="230"></text>
  <text id="author" class="author-text" x="492.5" y="300"></text>
  <text id="attribution" class="attribution-text" x="492.5" y="500">Inspirational quotes provided by ZenQuotes API</text>
  <script><![CDATA[
    async function fetchQuote() {
      try {
        const response = await fetch('https://zenquotes.io/api/random');
        const data = await response.json();
        const quote = data[0].q;
        const author = data[0].a;
        document.getElementById('quote').textContent = quote;
        document.getElementById('author').textContent = `— ${author}`;
      } catch (error) {
        document.getElementById('quote').textContent = 'Failed to load quote';
        document.getElementById('author').textContent = '';
      }
    }
    fetchQuote();
  ]]></script>
</svg>